var expect = require("chai").expect;
var util = require('../../utils/util');
var app = require('../../app').app;
var page = require('./reservation').page;
describe('测试 绘制时间条函数', function () {
    // let data_list = [{ "brand": "星海立式钢琴", "room_num": "F2-201", "unit_price": 15, "occupied_time": [] }];
    // let answer = [{ "width": 100, "color": "#AAAAAA" }]
    // it('没有预约的琴房', function () {
    //     expect(updateList(data_list)).to.be[0]["time"].equal(answer);
    // })
})